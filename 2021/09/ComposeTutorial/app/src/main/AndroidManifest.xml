<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.composetutorial">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ComposeTutorial">
        <activity
            android:name=".chipgroup.ChipGroupActivity"
            android:exported="false"
            android:label="@string/title_activity_chip_group"
            android:theme="@style/Theme.ComposeTutorial.NoActionBar" />
        <activity
            android:name=".chipgroup.ChipGroupMultiActivity"
            android:exported="false"
            android:label="@string/title_activity_chip_group_multi"
            android:theme="@style/Theme.ComposeTutorial.NoActionBar" />
        <activity
            android:name=".WebViewActivity"
            android:exported="true"
            android:label="@string/title_activity_web_view"
            android:theme="@style/Theme.ComposeTutorial.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

</manifest>